# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The full, authoritative guidance for this repository lives in [AGENTS.md](AGENTS.md) — commands (uv, ruff, mypy, pytest), entry points, architecture, data conventions, coding style, and commit/PR guidelines. Read it first.

Do not duplicate that content here; update `AGENTS.md` instead so the two never drift. Per-session and tool-specific notes (e.g. `.github/instructions/`) supplement, but do not override, `AGENTS.md`.
